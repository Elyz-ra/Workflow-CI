name: shipping_delay
conda_env: conda.yaml
environment:
  env_manager: local
entry_points:
  main:
    parameters:
      data_folder: {type: str}
    command: "python modelling.py --data_folder {data_folder}"