name: shipping_delay

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_folder: {type: str}
    command: "conda run --no-capture-output python modelling.py --data_folder {data_folder}"
